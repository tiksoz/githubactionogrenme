name: Test Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  DAY_OF_WEEK: Monday

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      SELAMLAMA: Hello
    steps:
      - name: Checkout Adimi
        uses: actions/checkout@v4
      - shell: bash
        env:
          SUPER_SECRET: ${{ secrets.DOCKER_USERNAME }}
        run: |
          echo "$SUPER_SECRET"

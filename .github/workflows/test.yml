name: Test Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  DAY_OF_WEEK: Monday

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      SELAMLAMA: Hello
    steps:
      - name: Checkout Adimi
        uses: actions/checkout@v4

      - shell: bash
        env:
          OSMAN: ${{ github.repository_owner }}
        run: |
          echo "$OSMAN"

name: Ilk Workflowumuz

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deneme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          ref: osman
      - name: Hello World
        run: |
          ls -al
          echo "Hello World"
          printenv

      - name: Osman
        uses:  gr2m/cowsay-action@v1.x
        with: 
          text: 'BBDevOpsBootcam Ekibi. UYUMA!!! '
          color: 'blue' 
